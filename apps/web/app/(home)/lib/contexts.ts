import { DialogContext, createDialogContext } from 'ui'

const productModal: DialogContext = createDialogContext()

const alertModal: DialogContext = createDialogContext()

export { alertModal, productModal }
